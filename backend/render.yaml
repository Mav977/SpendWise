services:
  - type: web
    name: expense-tracker-backend
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    branch: main
    autoDeploy: true
